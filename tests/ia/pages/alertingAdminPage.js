const assert = require('assert');
const { I } = inject();

module.exports = {
  url: 'graph/alerting/admin',
  elements: {
    configTextarea: `textarea[id='configuration']`,
  },
  buttons: {
  },
  messages: {
  },
}